/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.pinimg.com',
      'utfs.io', // Add the missing domain here
    ],
  },
};

export default nextConfig;
  